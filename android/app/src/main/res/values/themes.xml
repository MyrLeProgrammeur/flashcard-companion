<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose paints everything; the platform theme only needs to stay out of
         the way and avoid a white flash before the first frame. -->
    <style name="Theme.FlashcardCompanion" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/window_bg</item>
    </style>
</resources>
