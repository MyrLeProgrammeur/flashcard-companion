# Runtime deps — must build on Termux (Android / aarch64), the production target.
# pydantic-core and jiter compile from source there (needs `pkg install rust`);
# manylinux wheels are NOT usable on Termux (Bionic libc).
fastapi>=0.115.0
uvicorn>=0.30.0        # plain: the [standard] extras (uvloop/httptools/watchfiles) don't build on Termux
python-dotenv>=1.0.0
openai>=1.50.0
pyyaml>=6.0
pydantic>=2

# PDF grounding (pdf_context.py) — text extraction for the "besoin d'aide"
# feature. pypdf is pure Python and installs on Termux (aarch64/Bionic),
# unlike pdfplumber whose pypdfium2 dep has no Termux build. The extractor
# prefers pdfplumber where present (PC/dev) and falls back to pypdf on Termux.
pypdf>=4.0
# Optional on PC/dev for slightly better extraction: pip install pdfplumber
