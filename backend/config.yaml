paths:
  # Dossier où le pipeline PC écrit les .apkg (synchronisé via Syncthing).
  # Chemin absolu confirmé sur le tel (2026-07-01) — pas besoin de passer par
  # ~/storage/shared/ (symlink Termux), l'absolu marche directement une fois
  # termux-setup-storage exécuté (accès au stockage partagé accordé).
  apkg_dir: "/storage/emulated/0/syncthing/Flashcards"

  # Dossier des PDF de cours sources, synchronisé via Syncthing (même remarque).
  pdf_dir: "/storage/emulated/0/syncthing/Cours"

  # État propre de l'app (SRS, cache d'explications, matching PDF résolu).
  # Volontairement HORS de tout dossier Syncthing.
  data_dir: "~/flashcard-companion/data"

infercom:
  base_url: "https://api.infercom.ai/v1"
  explain_model: "DeepSeek-V3.1"

server:
  host: "127.0.0.1"
  port: 8420

explain:
  max_pdf_context_chars: 15000
